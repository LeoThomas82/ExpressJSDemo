extends Layout 

block content

    h1 #{title}
    p Please fill in the following form to login into system.

    form(method='POST')
        div.form-group
            label(for='username') Username:
            input#username.form-control(type='text', placeholder='UserID', name='username', autocomplete='off', required value=(undefined===username ? '' : username))

        div.form-group            
            label(for='password') Password: 
            input#password.form-control(type='password', placeholder='Password' name='password' autocomplete='off', required value=(undefined===password ? '' : password))

        button.btn.btn-primary(type='submit') Submit 

        if errors
            ul 
                for error in errors 
                    li!= error.msg