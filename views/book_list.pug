extends layout

block content
  h1= title
  if book_list.length > 0
    ul 
      each book in book_list 
        li 
          a(href=book.url) #{book.title} 
          | (#{book.author.name})
  else 
    p There are no books.